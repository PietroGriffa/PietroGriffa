# MOST USED ALIASES

# ranger to allow navigating between dirs
alias ranger='. ranger'

# Shotcut for disk usage
alias d='du -hs'

# Shortcut for safe copying
alias ccp='rsync -vrltD --stats --human-readable --info=progress2'